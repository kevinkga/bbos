# BBOS Backend Configuration

# Server Configuration
PORT=3001
HOST=localhost
NODE_ENV=development

# Frontend URLs for CORS
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:3000,http://localhost:3002

# Build System Configuration
BUILD_DIR=/tmp/bbos-builds
WORK_DIR=/tmp/bbos-work
DOWNLOAD_CACHE=/tmp/bbos-cache

# Demo Mode - Set to 'false' to enable real Armbian image downloads
# When true: Creates mock text files for development
# When false: Downloads real Armbian images from dl.armbian.com (requires internet and disk space)
DEMO_MODE=true

# Armbian Configuration
ARMBIAN_REPO=https://github.com/armbian/build.git

# Security (for production)
# JWT_SECRET=your-jwt-secret-here
# ENCRYPTION_KEY=your-encryption-key-here
